tags:
  - Contacts
summary: Delete contact
operationId: deleteContact
description: 'Delete a contact by id'
security:
  - bearerAuth: []
parameters:
  - in: path
    name: id
    schema:
      type: string
    required: true
    description: delete id of the contact
responses:
  '200':
    description: Contact deleted successfully
    content:
      application/json:
        schema:
          type: object
          properties:
            status:
              type: integer
              example: 200
            message:
              type: string
              example: Successfully deleted the contact!
  '401':
    $ref: '../../../components/responses/401.yaml'
